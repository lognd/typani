# Placeholder for T-0010 (typani-core: PyO3/maturin native Result/Option).
# Replace this pure-Python placeholder package with the real native
# extension (crates/typani-core/Cargo.toml, src/lib.rs). This stub exists
# ONLY so `uv lock` can resolve the `typani[native]` extra's local path
# source (pyproject.toml [tool.uv.sources]) before typani-core has a real
# PyPI release; it has no functionality of its own.
