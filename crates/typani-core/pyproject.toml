[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "typani-core"
version = "0.1.0"
description = "Placeholder for the typani-core native extension (T-0010)."
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.setuptools]
packages = ["typani_core"]
